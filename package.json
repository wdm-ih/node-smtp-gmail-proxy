{
  "name": "smtp-gmail-proxy",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "build": "npx esbuild index.js --bundle --platform=node --outfile=dist/smtp-proxy.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.8.4",
    "google-auth-library": "^9.15.1",
    "smtp-server": "^3.13.6",
    "winston": "^3.17.0"
  }
}
